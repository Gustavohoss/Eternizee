import { VoidCanvas } from '@/components/void-canvas';

export default function Home() {
  return (
    <main className="fixed inset-0 flex items-center justify-center bg-black cursor-none">
      <VoidCanvas />
    </main>
  );
}
